# Get your keys from https://dashboard.clerk.com → Your App → API Keys
CLERK_SECRET_KEY=

# Database (defaults to local SQLite)
# DATABASE_URL=sqlite:///./app.db

# YouTube URL ingest (yt-dlp + OAuth2 by default; Apify only if yt-dlp fails or INGEST_PROVIDER=apify)
INGEST_PROVIDER=auto
APIFY_API_TOKEN=
APIFY_YOUTUBE_ACTOR=streamers~youtube-video-downloader
APIFY_YOUTUBE_QUALITY=360
APIFY_YOUTUBE_TIMEOUT_SEC=300
YT_DLP_BIN=yt-dlp

# Gemini API Key (needed for high-quality transliteration)
GEMINI_API_KEY=

# Gemini text model for transliteration (and optionally B-roll planning).
TRANSLITERATION_GEMINI_MODEL=gemini-3.5-flash

# Indic-language B-roll pipeline: translate first, then generate visual queries.
# Existing TRANSCRIBE_SARVAM_API_KEY is also accepted, so you do not need two keys.
SARVAM_API_KEY=
BROLL_SARVAM_TRANSLATE_ENABLED=true
BROLL_SARVAM_TRANSLATE_MODEL=sarvam-translate:v1
# Use code-mixed if the transcript blends an Indic language and English.
BROLL_SARVAM_TRANSLATE_MODE=formal
BROLL_RETRIEVAL_PROVIDER=gemini
BROLL_GEMINI_MODEL=gemini-3.5-flash
BROLL_GEMINI_TIMEOUT_SEC=8
# A Sarvam-translated Indic beat already receives Gemini visual planning once.

# --- Vocal isolation (high-quality MelBand Roformer testing, branch tom) ---
# MelBand Roformer (vocals SDR 12.6) as primary, MDX23C as fallback.
TRANSCRIBE_VOCAL_ISOLATION_ENABLED=true
TRANSCRIBE_VOCAL_ISOLATION_HIGH_QUALITY=false
TRANSCRIBE_VOCAL_ISOLATION_HIGH_QUALITY_BACKEND=mdx23c
TRANSCRIBE_VOCAL_ISOLATION_FALLBACKS=mdx23c
TRANSCRIBE_VOCAL_ISOLATION_MODEL_MELBAND_ROFORMER=vocals_mel_band_roformer.ckpt
TRANSCRIBE_VOCAL_ISOLATION_TIMEOUT_SEC=420
TRANSCRIBE_VOCAL_ISOLATION_COMMAND_MELBAND_ROFORMER=audio-separator --model_filename {model} --single_stem Vocals --output_format WAV --output_dir {output_dir} --custom_output_names '{{"Vocals":"vocals"}}' {input}
TRANSCRIBE_VOCAL_ISOLATION_COMMAND_OUTPUT_MELBAND_ROFORMER=vocals.wav
