# Leave empty to use the same origin in production behind a reverse proxy.
# For local split-port development, Vite falls back to http://127.0.0.1:8000.
VITE_API_BASE=
VITE_REQUEST_TIMEOUT_MS=120000
VITE_FILLER_AGGRESSIVE_SINGLE_WORDS=false
