run = "cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000"

[env]
PYTHONUNBUFFERED = "1"
DATABASE_URL = "postgresql://user:password@localhost/clipmind"

[nix]
channel = "unstable"

[[ports]]
localPort = 8000
exposeNetwork = true
externalPort = 443
